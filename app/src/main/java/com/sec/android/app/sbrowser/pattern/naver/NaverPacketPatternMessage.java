package com.sec.android.app.sbrowser.pattern.naver;

import com.sec.android.app.sbrowser.engine.WebViewManager;

public class NaverPacketPatternMessage extends NaverPatternMessage {

    private static final String TAG = NaverPacketPatternMessage.class.getSimpleName();

    public NaverPacketPatternMessage(WebViewManager manager) {
        super(manager);
    }
}
